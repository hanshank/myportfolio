extends layout

block page content
  div.blog-posts-grid
    each post in posts
      a(href=`/blog/${post.slug}` class="post-preview-link")
        div.image-container
          img(src=`${post.image_url}`, alt="")
          h3.post-preview-title #{post.title}
        
